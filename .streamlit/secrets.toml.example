# This is an example secrets file for Streamlit Cloud
# On Streamlit Cloud, add these secrets via the web interface:
# https://share.streamlit.io → Your app → Settings → Secrets

OPENAI_API_KEY = "sk-your-openai-api-key-here"

# Optional: Add this if you use NCBI API in the future
# NCBI_API_KEY = "your-ncbi-api-key-here"
