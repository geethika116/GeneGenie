# Streamlit for the web app
streamlit==1.50.0

# PyMuPDF for PDF text extraction
PyMuPDF==1.22.5

# OpenAI client for GPT API
openai==1.29.0

# Pandas for CSV export
pandas==2.3.3

# Environment variable management (optional for local development)
python-dotenv==1.0.0

# Regular expressions, math, and other standard libraries are built-in, so no need to install

# Optional: add any other packages you use for processing or AI summaries
